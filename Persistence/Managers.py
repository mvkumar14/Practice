# these communicate with the database server
# which should be the piece that connects the
# queries in a personal sub-language/ flavor of SQLn

# Data management objects mediate between problem domain objecst and the database server object

# Data management objects are called by the problem domain objects, and in turn
# take those requests and translate them into the langauge
# that the database server object can understand?

# All the database management objects should refer to 
# a single instance of the database server object?

# Database API is where the actual code is written

# Databases can do many things, but the Database Server takes
# the subset of valuable reads and writes, and allows for them

